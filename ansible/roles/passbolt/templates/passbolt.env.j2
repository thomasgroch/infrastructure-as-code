# Passbolt Configuration
MYSQL_DATABASE=passbolt
MYSQL_USER=passbolt
MYSQL_PASSWORD={{ mysql_password | default(lookup('password', '/dev/null chars=ascii_letters,digits length=32')) }}

APP_DOMAIN={{ passbolt_domain }}

# SMTP Configuration (optional)
SMTP_HOST={{ smtp_host | default('smtp.gmail.com') }}
SMTP_PORT={{ smtp_port | default('587') }}
SMTP_USER={{ smtp_user | default('') }}
SMTP_PASSWORD={{ smtp_password | default('') }}
